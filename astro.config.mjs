// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	// TODO: replace with your GitHub Pages URL, e.g. https://<username>.github.io
	site: 'https://TODO-username.github.io',
});
